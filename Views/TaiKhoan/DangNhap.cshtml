@* 

 @model VNTour.ViewModel.DangNhapVM

@{
    ViewData["Title"] = "Đăng nhập";
}

<style>
    .register-section {
      padding: 4rem 0;
      background: #f8f9fa;
    }

    .register-form {
      background: #fff;
      padding: 3rem;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
    }

    .register-form .form-control {
      height: 50px;
      font-size: 16px;
      border-radius: 8px;
      margin-bottom: 20px;
      transition: 0.3s;
    }

    .register-form .form-control:focus {
      border-color: #28a745;
      box-shadow: none;
    }

    .register-form h2 {
      margin-bottom: 30px;
      font-weight: 700;
      color: #343a40;
      text-align: center;
    }

    .btn-register {
      width: 100%;
      font-size: 18px;
      padding: 12px 0;
      border-radius: 8px;
    }

    .hero-wrap {
      background-size: cover;
      background-position: center;
      position: relative;
      height: 50vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
    }

    .hero-wrap .overlay {
      position: absolute;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0,0,0,0.4);
      z-index: 1;
    }

    .hero-wrap .text-center {
      z-index: 2;
    }
</style>

<section class="register-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">

                <form asp-action="DangNhap" asp-controller="TaiKhoan" method="post" class="register-form">
                    <h2>Đăng nhập</h2>

                    <input asp-for="EmailOrPhone" class="form-control" placeholder="Email hoặc Số điện thoại" />
                    <input asp-for="MatKhau" type="password" class="form-control" placeholder="Mật khẩu" />

                    <button type="submit" class="btn btn-primary btn-register">Đăng nhập</button>
                    <div class="text-right mb-3">
                        <a asp-action="QuenMatKhau" asp-controller="TaiKhoan" class="text-muted">Quên mật khẩu?</a>
                    </div>
                    <div class="text-center mt-3">
                        <span>Chưa có tài khoản?</span>
                        <a asp-action="DangKy" asp-controller="TaiKhoan" class="text-primary font-weight-bold">Đăng ký</a>
                    </div>
                </form>

            </div>
        </div>
    </div>
</section>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
} *@

@model VNTour.ViewModel.DangNhapVM

@{
    Layout = null;
    ViewData["Title"] = "Đăng nhập";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">

    <div class="w-full max-w-4xl flex bg-white shadow-lg rounded-xl overflow-hidden">
        <!-- Left Side -->
        <div class="hidden md:flex w-1/2 items-center justify-center bg-gradient-to-br from-blue-300 via-green-200 to-white relative">
            <div class="text-gray-700 text-xl font-semibold opacity-20 absolute text-center">
                <span class="text-8xl">➤</span>
            </div>
        </div>

        <!-- Right Side -->
        <div class="w-full md:w-1/2 p-8 md:p-12">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800">Login</h2>
                <a asp-controller="TaiKhoan" asp-action="DangKy" class="text-sm text-blue-600 hover:underline">Don't have an account?</a>
            </div>

            <form asp-action="DangNhap" asp-controller="TaiKhoan" method="post" class="space-y-5">
                <!-- Email -->
                <div>
                    <label asp-for="EmailOrPhone" class="block text-sm font-medium text-gray-700 mb-1">Email or Phone</label>
                    <input asp-for="EmailOrPhone"
                           class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                           placeholder="you@example.com or 0123456789" />
                    <span asp-validation-for="EmailOrPhone" class="text-red-600 text-sm"></span>
                </div>

                <!-- Password -->
                <div>
                    <label asp-for="MatKhau" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                    <input asp-for="MatKhau" type="password"
                           class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                           placeholder="Nhập mật khẩu" />
                    <span asp-validation-for="MatKhau" class="text-red-600 text-sm"></span>
                </div>

                <!-- Submit -->
                <button type="submit"
                        class="w-full bg-blue-600 text-white py-3 px-4 rounded-lg font-semibold hover:bg-blue-700 transition">
                    Login
                </button>
            </form>

            <div class="mt-6 text-center text-sm text-gray-500">
                Quên mật khẩu <a asp-controller="TaiKhoan" asp-action="QuenMatKhau" class="text-blue-600 hover:underline">Quên Mật Khẩu</a>
            </div>
        </div>
    </div>

</body>
</html>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
